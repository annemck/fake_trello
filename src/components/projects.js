import React, {Fragment} from "react";

const ListProjects = ({user}) => {
  return (
    <Fragment>
      <h1>Hello world, this will be the users project list</h1>
    </Fragment>
  );
};

export default ListProjects;
